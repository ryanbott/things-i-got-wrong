.wrapper, .header, .footer
  max-width: 1000px
  margin 0 auto
  &--text
    background white
    padding 2rem
    font-size 1.7rem
  @media (max-width: 1000px)
    padding 0 2rem

body
  background bluegrad
  border-top 4px solid orange1
  min-height: calc(100vh - 16px);
